// Auth is handled per-route in Route Handlers and per-page via client-side
// useAuth hook. This middleware is a no-op placeholder.
export function middleware() {}
export const config = { matcher: [] };
